spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/mybatis-plus?useSSL=false&characterEncoding=UTF-8&serverTimezone=GMT%2B8
    username: root1
    password: root

logging:
  level:
    root: warn
    com.easondongh.dao: trace
  pattern:
    console: "%p%m%n"

mybatis-plus:
  global-config:
    db-config:
      logic-not-delete-value: 0
      logic-delete-value: 1
